const nextConfig = {
  output: "export",
  basePath: "/Portfolio",
  assetPrefix: "/Portfolio/",
  images: {
    unoptimized: true,
  },
  transpilePackages: ['@splinetool/react-spline'],
};

export default nextConfig;
